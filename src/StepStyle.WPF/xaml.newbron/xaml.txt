 <Grid>

     <Grid.ColumnDefinitions>
         <ColumnDefinition Width="*"/>
         <ColumnDefinition Width="2*"/>
     </Grid.ColumnDefinitions>
     <Grid Grid.Column="1" Background="White">
         
         <Grid.RowDefinitions>
             <RowDefinition/>
             <RowDefinition Height="7*"/>
         </Grid.RowDefinitions>
        
         <Grid Grid.Column="1" Background="#667AEA">
             <ListBoxItem FontSize="25" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold" VerticalAlignment="Center">Простмотр забронированного</ListBoxItem>
         </Grid>
     </Grid>
     <Grid >
         <Grid.RowDefinitions>
             <RowDefinition/>
             <RowDefinition Height="4*"/>
         </Grid.RowDefinitions>
         <Image Source="/resources/icon.png" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="UniformToFill"/>
         <Grid Grid.Row="1" Background="#667AEA">
             <ListBox>
                 <ListBoxItem Foreground="White" FontSize="18" Margin="3" FontWeight="Bold">Новая бронь</ListBoxItem>
                 <ListBoxItem Foreground="White" FontSize="18" Margin="3" FontWeight="Bold">Снять бронь</ListBoxItem>
                 <ListBoxItem Foreground="White" FontSize="18" Margin="3" FontWeight="Bold">Редактирование брони</ListBoxItem>
                 <ListBoxItem Foreground="White" FontSize="18" Margin="3" FontWeight="Bold">Информация для клиентов</ListBoxItem>
                 <ListBoxItem Foreground="White" FontSize="18" Margin="3" FontWeight="Bold">Редактирование расписания</ListBoxItem>
             </ListBox>
         </Grid>
     </Grid>
 </Grid>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
     <Window.Resources>
        <!-- Определяем стиль кнопки -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC" />
            <!-- Цвет фона -->
            <Setter Property="Foreground" Value="White" />
            <!-- Цвет текста -->
            <Setter Property="FontSize" Value="16" />
            <!-- Размер шрифта -->
            <Setter Property="Padding" Value="10,5" />
            <!-- Внутренние отступы -->
            <Setter Property="BorderBrush" Value="#FF007ACC" />
            <!-- Цвет обводки -->
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Effect" Value="{x:Null}" />
            <!-- Default: no shadow -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <!-- Радиус скругления -->
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- Стиль при наведении -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF005999" />
                    <!-- Изменение цвета при наведении -->
                    <Setter Property="BorderBrush" Value="#FF005999" />
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="5" Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
   <Grid>
      <Border MinWidth="400" Margin="15" Background="LightCyan" VerticalAlignment="Center"
   Padding="30" MaxWidth="550" CornerRadius="20" Grid.Column="1">
          <Border.Effect >
              <DropShadowEffect BlurRadius="30" Color="LightGray" ShadowDepth="0" ></DropShadowEffect>
          </Border.Effect>
          <StackPanel>
              <Button Style="{StaticResource RoundedButtonStyle}" Margin="20" FontSize="20" FontWeight="Bold" Content="Двухместный" Width="300" Height="50"/>
              <Button Style="{StaticResource RoundedButtonStyle}" Margin="20" FontSize="20" FontWeight="Bold" Content="Трехместный" Width="300" Height="50"/>
              <Button Style="{StaticResource RoundedButtonStyle}" Margin="20" FontSize="20" FontWeight="Bold" Content="Четырёхместный" Width="300" Height="50"/>
          </StackPanel>
      </Border>
  </Grid>